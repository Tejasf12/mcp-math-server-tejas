# MCP Math Server Configuration

# API Key for authentication (required)
# Generate a secure random string for production use
MCP_API_KEY=enter-your-secret-key

# Server port (optional, defaults to 3000)
PORT=3000
